# docker-compose.override.yml.example
# Copy this file to docker-compose.override.yml and customize as needed

# This file provides optional overrides for development environment
# Use it to enable Docker-in-Docker or other advanced features

version: '3.8'

services:
  dev-env:
    # Uncomment to enable Docker-in-Docker (less secure)
    # volumes:
    #   - /var/run/docker.sock:/var/run/docker.sock:ro
    
    # Uncomment to enable privileged mode (for systemd or Docker-in-Docker)
    # privileged: true
    
    # Additional environment variables
    environment:
      # Enable Docker-in-Docker if needed
      # - ENABLE_DOCKER_IN_DOCKER=true
      
      # Custom environment variables
      # - CUSTOM_VAR=value
    
    # Additional port mappings
    # ports:
    #   - "3000:3000"  # Custom application port
    #   - "5432:5432"  # Database port (if needed)
    
    # Resource limits override (increase if needed)
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '4.0'
    #       memory: 4G
    
    # Additional labels
    labels:
      - "custom.environment=development"
      
  # Override Redis configuration if needed
  redis:
    # ports:
    #   - "6379:6379"  # Expose Redis port externally
    
    # command: >
    #   redis-server
    #   --appendonly yes
    #   --maxmemory 256mb
    #   --requirepass mypassword
      
  # Enable PostgreSQL by uncommenting
  # postgres:
  #   ports:
  #     - "5432:5432" 